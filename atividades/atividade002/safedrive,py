# A empresa "SafeDrive" está desenvolvendo um sistema de monitoramento de velocidade para ajudar a promover a segurança nas estradas.
#  Eles precisam de um programa que permita aos usuários inserir a velocidade de um carro e, em seguida, exibir na tela uma mensagem 
# adequada com base na velocidade fornecida. O objetivo principal é alertar os motoristas caso ultrapassem o limite de velocidade de 60 km/h,
#  incentivando-os a dirigir dentro dos limites legais e, assim, reduzir o risco de acidentes.

import os


os.system('cls')

print('-' * 90)
print('Safe Drive Company.')
print('=' * 90)

velocidade = float(input('ENTRE COM A VELOCIDADE: '))

print('-' * 90)

if velocidade > 60:
    print(f'A {velocidade} é maior que o permitido, reduza.')
elif velocidade < 60:
    print(f'A {velocidade} é permitida.')
else:
    print('Velocidade está no limite, atenção.')

print('.' * 90)